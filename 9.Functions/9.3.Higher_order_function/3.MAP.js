/* We will use MAP , to operate some functionality in each element of array */
/* the fxnality can be like to divide every elemnet of array by 2 or find square of every element of array or etc*/
/* note that MAP does not modify the source aray, the modification is done in new array


/*
we will use syntax1 mostly
syntax1:

updatedarray = arr.map((element, index, arr) => {
                  state.1
                  state.n
                return updatedelement
              })

*/

/*
syntax2:

let fun = (element, index, arr) => {
                  state.1
                  state.n
                return updatedelement
              }
arr.map(fun)

*/
