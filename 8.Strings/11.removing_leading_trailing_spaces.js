// remove both leading and trailing spaces use trim()
//remove only leading spaces use trimStart()
//remove only trailing spaces use trimEnd()]

let s = "     sd hs   dsd   s     "
console.log(s.trim())
console.log(s.trimStart())
console.log(s.trimEnd())
